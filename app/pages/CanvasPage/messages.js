/*
 * CanvasPage Messages
 *
 * This contains all the text for the CanvasPage container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CanvasPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CanvasPage container!',
  },
});
